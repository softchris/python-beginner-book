# what is list comprehension?
# list comprehension is a way to create a list using a for loop in one line
# show me code for a list comprehension
# l = [1, 2, 3, 4, 5]
# l2 = [i for i in l]
# print(l2)
# # show output from code above
# # [1, 2, 3, 4, 5]
# what's the benefit of list comprehension?
# it is a way to create a list from another list
# it is a way to create a list from another list with a condition
# show me code for a list comprehension with a condition
# l = [1, 2, 3, 4, 5]
# l2 = [i for i in l if i % 2 == 0]
# any drawbacks with list comprehension?
# it can be difficult to read
# it can be difficult to debug